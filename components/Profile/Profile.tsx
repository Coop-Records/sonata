'use client';

import { useEffect } from 'react';

const Profile = () => {
  
  useEffect(() => {
    const init = async () => {
     
    };
    init();
  }, []);

 
  return (
    <div className="w-full space-y-4">
      
    </div>
  );
};

export default Profile;
